name: 🚀 Build mSearch (Electron)

on:
push:
branches: [main, master]
workflow_dispatch:

jobs:
build:
name: ${{ matrix.os }}
runs-on: ${{ matrix.os }}

```
strategy:
  fail-fast: false
  matrix:
    os:
      - windows-latest
      - macos-latest
      - ubuntu-latest

steps:
  - name: 📥 Clone repo
    uses: actions/checkout@v4

  - name: 🟢 Setup Node
    uses: actions/setup-node@v4
    with:
      node-version: 20
      cache: npm

  - name: 📦 Install deps
    run: npm ci

  - name: 🛠 Build Electron
    run: npm run build

  - name: 📤 Upload artifacts
    uses: actions/upload-artifact@v4
    with:
      name: msearch-${{ matrix.os }}
      path: dist/
```
